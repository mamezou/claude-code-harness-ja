---
name: work-log
description: 当日の作業ログ work-log-YYYYMMDD.md を案件の書式で作成・追記する。作業終了時または方針確定時に呼び出す (応答ごとには更新しない)。案件は引数で指定する。週次サマリーは対象外。
argument-hint: <案件名>
---

# 作業ログの作成・追記

置き場: `<案件>/docs/logs/work-log-YYYYMMDD.md` (日付は当日)。当日ファイルが
既にあれば末尾に H2 節を追記し、なければ新規作成する。
案件は引数で指定する (省略された場合は対象の案件を確認してから書く)。

## 書式

- 1 行目は `# 作業ログ YYYY-MM-DD`。H1 はこの 1 つだけ
- H2 は作業単位ごとに時系列順。見出しは体言止め (「〜の改稿」「〜への反映(N件)」
  「〜の確認」)。固定の節構成はなく、その日の作業数だけ並べる
- H3 は 1 つの H2 内に複数の下位項目があるときだけ使う
- 中身はすべて箇条書き。過去形の報告文 (「〜した」「〜を確認した」) で書き、
  段落文は使わない
- 依頼者の発言・判断はラベル付きで残す: 「依頼者の指摘:」「依頼者の判断:」
  「依頼者の承認:」。ほかのラベルは「経緯:」「補足:」「対象:」
- ファイルパス・コミットハッシュ・短いコマンドはインラインコード。複数行の
  コマンドだけ bash コードブロックにし、置換箇所は `<...>` で示す。パスは相対表記
- 時刻 (HH:MM) は書かない。日付は「7月30日」のように文中に書く

## 定型の記録

- ファイル変更: 対象パスと、新規作成か既存編集かを書く
- Notion 反映: 対象ページ → 直前に本文取得・一致確認 → 検索置換 N 箇所 (全文置換は
  不使用) → 再取得で確認、または再取得を省き「更新要求受理」として記録 →
  未対応の発見は「発見(未対応、依頼者の判断待ち):」で別出しする
- 送付ドラフト: 作成時は「`drafts/` 配下へ作成した。未送付」。送付確認後は
  「`archive/sent/` へ移動した」
- 各作業ブロックの末尾に、実施していないことを明記する:
  「コミット、Pushは行っていない」「クラウド環境、PDF、成果物リポジトリ、コミット、
  Pushは変更していない」等。節として立てる場合の節名は「実施していないもの」

## 手順

1. 当日の会話とツール実行の記録から、作業単位 (H2 相当) を抽出する
2. 上記書式で本文を作り、Write する (既存ファイルへは末尾追記)
3. 報告には、対象パス、新規か追記かの別、追記した節の一覧を含める

## 記録の原則

- 事実のみを書く。推測は「(推測)」と明記する
- 完了と未完了を混ぜない。未実施・判断待ちはその旨を書く
- 週次サマリー (weekly-summary) は本 Skill の対象外 (書式を定義していない。
  作る場合は依頼者と書式を決めてから)
